[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    slow: Tests that take more than 1 second to run
    performance: Benchmarking tests measuring execution speed